{
  "name": "@chitapp/rules-tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project rules-test \"vitest run\""
  },
  "dependencies": {
    "@firebase/rules-unit-testing": "^4.0.0"
  },
  "devDependencies": {
    "firebase-tools": "^14.0.0",
    "typescript": "~6.0.2",
    "vitest": "^3.2.4"
  }
}
