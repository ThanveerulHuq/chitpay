# Local/deployment configuration. Do not commit real credentials.
KWIC_API_KEY=

# Optional overrides. The default push URL matches the Kwic endpoint supplied
# for ChitPay. Keep the header image unset for the text-only templates.
KWIC_PUSH_URL=https://app.kwic.in/api/v1/push
KWIC_HEADER_IMAGE_URL=
